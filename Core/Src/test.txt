				/*
				length = sprintf(buffer, "%lu,%lu,%lu,%lu,%lu\r\n",usb_buffer[0][0][i], usb_buffer[0][1][i], usb_buffer[0][2][i], usb_buffer[0][3][i], usb_buffer[0][4][i]);
				send_data_over_usb(buffer, length);
				uint8_t status;
				do {
					status = CDC_Transmit_FS((uint8_t*)buffer, sizeof(buffer));
				} while (status != USBD_OK);
*/


		//char buffer[64];
		/*
		int length =0;
		if(buffer_ready_flag == 0)
		{
			for(uint8_t i = 0; i<USB_BUFFER_SIZE;i++)
			{

				uint32_t header = 0xddccbbaa;
				uint32_t values[6] = {header,usb_buffer[0][0][i], usb_buffer[0][1][i], usb_buffer[0][2][i], usb_buffer[0][3][i], usb_buffer[0][4][i]};
				length = sizeof(values);
				//CDC_Transmit_FS((uint8_t*)values, length);

				uint8_t status;
				do {
					status = CDC_Transmit_FS((uint8_t*)values, sizeof(values));
				} while (status != USBD_OK);

			}
			buffer_ready_flag = 3;
		}
		else if(buffer_ready_flag == 1)
		{
			for(uint8_t i = 0; i<USB_BUFFER_SIZE;i++)
			{

				uint32_t header = 0xddccbbaa;
				uint32_t values[6] = {header,usb_buffer[1][0][i], usb_buffer[1][1][i], usb_buffer[1][2][i], usb_buffer[1][3][i], usb_buffer[1][4][i]};
				length = sizeof(values);
				//CDC_Transmit_FS((uint8_t*)values, length);

				uint8_t status;
				do {
					status = CDC_Transmit_FS((uint8_t*)values, sizeof(values));
				} while (status != USBD_OK);


			}
			buffer_ready_flag = 3;
		}
		else
		{}*/